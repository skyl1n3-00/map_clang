#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include "map_ss_functions.h"

void main(){
  map_string_string_ll *list = (map_string_string_ll*)malloc(sizeof(map_string_string_ll));
  free(list);
}
